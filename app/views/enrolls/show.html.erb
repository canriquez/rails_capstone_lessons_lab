<div class="card text-center">

  <div class="card-body d-flex flex-column align-items-center">
    <div class="confirmation-svg"><%= svg('check_ok') %></div>
    <h5 class="card-title">Enrollment Success for <%= @enroll.student.name %>.</h5>
    <p class="card-text">  Enrollment is confirmed for the course : <%= @enroll.course.name %>.</p>
       <%= link_to groups_path do %>
          <span class=" btn btn-primary">Go back to courses page</span>
          <% end %>
        </button>
  </div>

</div>